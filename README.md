# Ankit Prajapati — Data Analyst Portfolio

Open `index.html` in a browser to preview the website.

To publish:
1. Replace `resume.pdf` with the final resume PDF.
2. Upload the folder to GitHub Pages, Netlify, Vercel, or another static host.
3. Optional: connect a custom domain.

The site is responsive and uses no build system.
